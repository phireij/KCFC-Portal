# KCFC Portal Redevelopment Progress — 2026-09-10

## Branch

`redesign/mobile-first-v2`

Draft PR: #1 — **Redevelopment: mobile-first shell and schedule foundation**

Production `main` remains unchanged.

## Completed in the active redevelopment branch

### 1. Responsive application shell

- New official KCFC navy / royal-blue visual foundation.
- Mobile navigation is exactly five primary destinations: Home, Schedule, Community, Updates, More.
- Horizontal scrolling navigation has been removed from the new shell.
- Desktop uses a persistent left sidebar with role-aware utility and leadership links.
- Mobile More opens as a bottom sheet and contains Availability & Polls, Resources, Inbox, Profile, Accounting when authorized, and Leadership/Admin when authorized.
- Reading-size defaults were normalized toward the approved 15–17 px body range while preserving user-selectable larger accessibility sizes.

### 2. Schedule experience

- Existing detailed duties/assignment page is preserved as `LegacyDuties.tsx` for regression safety.
- `/duties` is a member-oriented Schedule hub.
- **All schedule** shows chronological Mass dates and expandable published ministry rosters.
- **My ministry** shows the member's own liturgical assignments and community duties in one timeline.
- **Plan & manage / Availability & duties** keeps the existing assignment workspace available during migration.
- Legacy notification/deep links containing `tab` or `pollId` still open the detailed workspace.
- Whole-schedule browsing includes Upcoming / all-date controls, search, liturgical ministry filters, a dedicated Where I Serve filter, empty-state recovery, month grouping and expandable roster detail.
- My Ministry has separate **Upcoming** and **History** views.
- Published-roster notifications can deep-link directly to `My ministry`.
- A dedicated schedule-browsing UX document records member, mobile, desktop, privacy and future-enhancement rules.

### 3. Home experience

- Existing dashboard is preserved as `LegacyDashboard.tsx` for reference/regression safety.
- Home is member-first rather than admin-statistics-first.
- Home highlights next published assignment, next KCFC Mass, availability-response status, action-needed availability, leadership approval alerts, upcoming Mass dates, announcements and quick links.
- No destructive database actions are present in the new member Home surface.

### 4. Liturgical availability and assignment workflow

- Existing poll implementation is preserved as `LegacyPolls.tsx` for compatibility.
- The redesigned member experience uses member-friendly language centered on **availability to serve**.
- Multi-Mass availability requests remain compatible with existing Firestore poll and response records.
- Members can select every Mass where they are available and revise their response while the request remains active.
- Leader mode includes request creation, response-progress monitoring, availability matrix, assignment builder grouped by Mass/ministry, availability-constrained candidate lists, explicit final roster publication and unpublish.
- New availability cycles use explicit publication state so completed internal planning is no longer treated as member-visible publication.
- Editing an assignment after publication automatically returns the roster to unpublished state, requiring deliberate review and re-publication.
- Final publication creates Portal notifications for assigned members.
- New Schedule surfaces display explicit-publication rosters only; legacy records retain a compatibility fallback so historical schedules do not disappear during migration.

### 5. Community Directory

- Community Directory uses the mobile-first KCFC design language.
- Search, Core/Regular member filtering and multi-ministry filtering are cleaner on phones.
- General directory cards do not expose private member contact details.

### 6. Updates / Announcements

- Updates is rebuilt in the new mobile-first design language.
- Announcement browsing, search, status clarity and editor ergonomics are improved.
- Audience and channel metadata groundwork supports future Public / Parishioner / KCFC Member / Leadership publishing.
- Public website synchronization remains inactive until an explicit production approval gate.

### 7. KCFC Inbox

- Inbox is the durable source of truth for operational communication.
- Mobile uses a message-list → detail flow; desktop retains a split-view experience.
- Search and category filters cover announcements, duties, broadcasts and system messages.
- Unread counts and Mark All Read remain available.
- Messages support deep links back to their relevant Portal workflow.
- New routing metadata can show urgency, source type and delivery channels while older notification records remain compatible.

### 8. Profile, communication preferences and notification health

- Profile is rebuilt in the approved visual language.
- General private profile fields remain editable without exposing them in the public member directory.
- Communication preferences distinguish announcements, liturgical availability, published assignments, community duties, broadcasts and urgent notices.
- Email is the default partner to PWA push.
- App & Notification Status shows installed/browser state, browser permission, endpoint count, health state, repair action and a guided **Install KCFC → Enable Alerts → Send Test** flow.
- Notification Health reads browser-safe connector feature gates and can distinguish Connected, Available to connect, Coming next and Future without exposing any provider credentials.
- The old olive-era install/notification panel was replaced with a focused navy/royal-blue **Install KCFC Portal** component.
- iPhone/iPad receives explicit Safari Share → Add to Home Screen guidance; Android/Chromium receives one-tap install when available plus browser-menu fallback.
- Installation no longer duplicates notification-registration logic; notification enable/repair/test remains centralized in Notification Health.
- Connected Communication Apps has an explicit member-facing foundation for LINE, Telegram, WhatsApp and Viber.
- Per-provider alert toggles remain disabled until a secure account link exists.
- Optional messaging apps remain non-mandatory.

### 9. Resources library

- Resources is rebuilt as a mobile-first searchable library.
- Members can search by title, description, ministry or file type.
- Ministry filters cover General, Choir, Lector & Commentator, Usher, Altar Server, Kitchen, Cleaning and Other.
- Empty-search recovery is explicit.
- Authorized leaders can add trusted URLs/document references from a phone-friendly editor.
- Existing resource records remain compatible; resources remain authenticated/private unless future public publishing is explicitly approved.

### 10. Treasury & Accounting migration

- The original accounting engine is preserved byte-for-byte as `LegacyAccounting.tsx` for regression safety.
- `Accounting.tsx` provides the KCFC navy/royal-blue role-aware leadership shell.
- Existing transaction creation/editing, category management, approval, receipts, exports, charts and historical data continue through the preserved engine.
- No financial permission or data model changes were introduced by this visual migration.

### 11. Leadership & Admin migration

- The original administration engine is preserved byte-for-byte as `LegacyAdmin.tsx`.
- `Admin.tsx` provides a leadership-oriented KCFC shell while preserving existing operations.
- The shell emphasizes review queues, least-privilege role management, careful broadcasting and preservation of member records.
- No production broadcast, user deletion or role migration is triggered by the redesign.

### 12. Communications architecture and routing policy

The approved routing hierarchy remains:

1. KCFC Inbox — durable source of truth.
2. PWA/Web Push — primary alert channel.
3. Email — default partner channel.
4. LINE — first optional Japan-focused secondary connector.
5. Telegram — additional optional connector.
6. WhatsApp — future/conditional.
7. Viber — low priority.
8. SMS — optional paid escalation only.

New implementation foundations:

- `src/lib/communicationRouting.ts` centralizes recipient-level channel planning without sending anything or reading credentials.
- `src/lib/notificationRecord.ts` builds normalized Inbox payloads with source, urgency and de-duplicated channel metadata.
- Canonical communication source/provider types now live in `src/types.ts`.
- Inbox is always retained; push and email respect event/member preferences; external providers require caller enablement + opt-in + connected state.
- `src/lib/communicationConnectorFlags.ts` exposes browser-safe availability flags only.
- Server and browser connector flags are all disabled by default in `.env.example`.
- CI verifies those defaults and rejects provider secret-style `VITE_*` variables.
- `communication-event-metadata.md` defines normalized source, urgency, channels and delivery-diagnostic metadata plus the migration order for current notification creators.
- Secure account-linking contracts remain documented for LINE / Telegram and future providers; no connector has been activated.

### 13. Authentication / registration experience

- Login, registration and password-reset screens were rebuilt in the approved KCFC navy/royal-blue mobile-first design.
- Desktop gets a KCFC community/feature panel while phone/tablet uses a focused single-column authentication card.
- Existing email/password sign-in, Google sign-in, registration, password reset and verification-email behavior remain available.
- Existing pending/pre-registered profile migration continues to preserve the authenticated Firebase UID and member record.
- Bootstrap-admin behavior remains compatible.
- New registration continues to require coordinator verification for normal member access.
- Authentication copy now makes it explicit that existing members keep the same account/member record.
- Placeholder avatar styling now uses KCFC navy instead of the old olive identity in the redesigned authentication surface.
- The authentication redesign passed TypeScript, production build and connector-safety CI.

## Specification baseline

- **Specification v4 — Communications + Schedule + Staging Safeguards** is now the current working redevelopment baseline.
- The final v4 DOCX render contains 28 pages.
- All 28 pages were visually inspected after the final pagination/layout update; no clipping, overlap, broken tables, missing glyphs or header/footer collisions were observed.
- `specification-v4-release-notes.md` records the implementation alignment and change-control policy.

## Staging preparation

- `staging-readiness-checklist.md` defines the release gates.
- `staging-test-matrix.md` converts them into repeatable synthetic-account scenarios for auth, navigation, Schedule, availability/assignment, Inbox, PWA, connectors, Resources, Accounting, Admin and accessibility.
- Staging evidence must not contain production personal data.

## Data handling

The redevelopment continues to use existing Firebase Auth identities and current Firestore records. No user recreation or destructive migration is introduced.

Current compatibility sources include `users`, `polls`, `polls/{pollId}/responses`, `duties`, `announcements`, `notifications`, `resources`, `accounting` and `accounting_categories`.

All new communication fields are optional so historical Firestore documents remain readable.

## Validation

- Redevelopment CI runs TypeScript validation and the production build on branch pushes and draft-PR updates.
- CI caught the first connector feature-gate implementation missing Vite `ImportMeta.env` typings; `src/vite-env.d.ts` fixed it and subsequent CI returned green.
- Connector safety guards are part of CI; current validation confirms TypeScript, production build, connector-default guard and browser-secret guard all pass through the authentication redesign.
- Production `main` remains at the original redevelopment baseline SHA `653cc7229600fd7baff17a21a21f12d267b66d2b`; there is currently no upstream drift to reconcile.
- Production merge remains blocked regardless of CI until explicit approval.

## Next work

1. Adopt the central communication routing metadata progressively in announcement, availability, assignment and broadcast notification creators without rewriting historical records.
2. Continue accessibility, mobile-device and regression QA.
3. Continue leadership workflow decomposition so Admin and Accounting can eventually move beyond preserved legacy engines without risking current operations.
4. Work through the staging-readiness checklist with isolated test users/data before requesting any production merge/deployment approval.
5. Keep public website synchronization and live external connectors disabled until their separate approval gates.

## Approval gates retained

No production merge, production deployment, destructive migration, user deletion, mass outbound notification, external connector activation or public website cutover will occur without an explicit approval gate.
