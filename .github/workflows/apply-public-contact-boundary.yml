name: Apply public contact boundary hardening

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-public-contact-boundary.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: python3 scripts/patch-public-contact-boundary.py
      - run: npx tsx scripts/verify-public-contact-boundary.ts
      - run: npm run lint
      - run: git diff --check
      - run: |
          rm scripts/patch-public-contact-boundary.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/verify-public-contact-boundary.ts scripts/patch-public-contact-boundary.py
          git commit -m 'fix: harden public contact ingestion boundary'
          git push origin HEAD:redesign/mobile-first-v2
