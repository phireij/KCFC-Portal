name: Apply server diagnostic privacy hardening

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-server-log-privacy.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - run: python3 scripts/patch-server-log-privacy.py
      - run: npx tsx scripts/verify-server-diagnostic-privacy.ts
      - run: git diff --check
      - run: |
          rm scripts/patch-server-log-privacy.py
          git config user.name 'github-actions[bot]'
          git config user.email '4189828+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/verify-server-diagnostic-privacy.ts scripts/patch-server-log-privacy.py
          git commit -m 'fix: remove member identifiers from server diagnostics'
          git push origin HEAD:redesign/mobile-first-v2

# retrigger after count correction
