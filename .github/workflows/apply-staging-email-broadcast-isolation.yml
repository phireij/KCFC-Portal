name: Apply staging email broadcast isolation

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-staging-email-broadcast-isolation.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - run: python3 scripts/patch-staging-email-broadcast-isolation.py
      - run: npx tsx scripts/verify-staging-email-broadcast-isolation.ts
      - run: git diff --check
      - run: |
          rm scripts/patch-staging-email-broadcast-isolation.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/patch-staging-email-broadcast-isolation.py
          git commit -m 'fix: isolate staging email broadcasts from SMTP'
          git push origin HEAD:redesign/mobile-first-v2
