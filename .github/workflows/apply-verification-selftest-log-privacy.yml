name: Apply verification and self-test log privacy fix

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-verification-selftest-log-privacy.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - run: python3 scripts/patch-verification-selftest-log-privacy.py
      - run: npx tsx scripts/verify-server-diagnostic-privacy.ts
      - run: git diff --check
      - run: |
          rm scripts/patch-verification-selftest-log-privacy.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/patch-verification-selftest-log-privacy.py
          git commit -m 'fix: redact verification and self-test diagnostics'
          git push origin HEAD:redesign/mobile-first-v2
