name: Apply broadcast content privacy fix

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-broadcast-content-privacy.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - run: python3 scripts/patch-broadcast-content-privacy.py
      - run: git diff --check
      - run: |
          rm scripts/patch-broadcast-content-privacy.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/patch-broadcast-content-privacy.py
          git commit -m 'fix: stop persisting broadcast message content in diagnostics'
          git push origin HEAD:redesign/mobile-first-v2
