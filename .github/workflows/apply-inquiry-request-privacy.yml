name: Apply inquiry request privacy guard

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-inquiry-request-privacy.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - run: python3 scripts/patch-inquiry-request-privacy.py
      - run: npx tsx scripts/verify-inquiry-privacy-logging.ts
      - run: git diff --check
      - run: |
          rm scripts/patch-inquiry-request-privacy.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/verify-inquiry-privacy-logging.ts scripts/patch-inquiry-request-privacy.py
          git commit -m 'fix: stop logging raw public inquiry requests'
          git push origin HEAD:redesign/mobile-first-v2
