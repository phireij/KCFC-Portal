name: Apply notification error response privacy fix

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-notification-error-response-privacy.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - run: python3 scripts/patch-notification-error-response-privacy.py
      - run: npx tsx scripts/verify-notification-error-response-privacy.ts
      - run: git diff --check
      - run: |
          rm scripts/patch-notification-error-response-privacy.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/verify-notification-error-response-privacy.ts scripts/patch-notification-error-response-privacy.py
          git commit -m 'fix: keep notification backend errors server-side'
          git push origin HEAD:redesign/mobile-first-v2
