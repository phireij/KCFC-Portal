name: Apply self-test log privacy fix

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-self-test-log-privacy.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - run: python3 scripts/patch-self-test-log-privacy.py
      - run: npx tsx scripts/verify-server-webpush-privacy.ts
      - run: git diff --check
      - run: |
          rm scripts/patch-self-test-log-privacy.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/patch-self-test-log-privacy.py
          git commit -m 'fix: remove member identity from push self-test logs'
          git push origin HEAD:redesign/mobile-first-v2
