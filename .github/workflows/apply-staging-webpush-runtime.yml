name: Apply staging Web Push runtime fix

on:
  push:
    branches: [redesign/mobile-first-v2]
    paths:
      - '.github/workflows/apply-staging-webpush-runtime.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/mobile-first-v2
          fetch-depth: 0
      - run: python3 scripts/patch-staging-webpush-runtime.py
      - run: npx tsx scripts/verify-staging-webpush-runtime.ts
      - run: git diff --check
      - run: |
          rm scripts/patch-staging-webpush-runtime.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server.ts scripts/patch-staging-webpush-runtime.py
          git commit -m 'fix: fail closed on staging Web Push credentials'
          git push origin HEAD:redesign/mobile-first-v2
